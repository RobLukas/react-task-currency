import styled from 'styled-components';

const Wrapper = styled.div`
  flex-grow: 1;
`;

const NoTransactionTitleStyled = styled.p`
  font-style: italic;
`;

const LargestTransactionTitleStyled = styled.p`
  margin-bottom: 40px;
`;

export { Wrapper, NoTransactionTitleStyled, LargestTransactionTitleStyled };
